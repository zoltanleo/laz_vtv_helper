### USER

Расскажи подробно о TPair<TKey, TValue, TInfo> из unit Generics.Collections для fpc/lazarus. Покажи подробные примеры работы с ним с подробными пояснениями 

### ASSISTANT

`TPair<TKey, TValue, TInfo>` — это **generic-запись (record)** в Free Pascal (FPC) / Lazarus, определённая в модуле `Generics.Collections`. Она позволяет хранить **три значения** разных типов:

- `Key` — ключ (тип `TKey`)
- `Value` — значение (тип `TValue`)
- `Info` — дополнительная информация (тип `TInfo`)

Это расширение обычной пары `TKey`, `TValue`, добавляя третий элемент `TInfo`.

---

## 🔍 Объявление и структура

```pascal
type
  TPair<TKey, TValue, TInfo> = record
    Key: TKey;
    Value: TValue;
    Info: TInfo;

    constructor Create(const AKey: TKey; const AValue: TValue; const AInfo: TInfo);
  end;
```

Как видите, у `TPair` есть:

- Три поля: `Key`, `Value`, `Info`
- Конструктор `Create`, чтобы удобно создавать экземпляры

---

## 📌 Пример использования

### ✅ Создание и заполнение TPair

```pascal
program PairExample;

{$mode objfpc}{$H+}

uses
  Classes, SysUtils, Generics.Collections;

var
  MyPair: TPair<String, Integer, Boolean>;

begin
  // Создаём пару: имя, возраст, активен ли пользователь
  MyPair := TPair<String, Integer, Boolean>.Create('Иван', 30, True);

  WriteLn('Имя: ', MyPair.Key);      // Иван
  WriteLn('Возраст: ', MyPair.Value); // 30
  WriteLn('Активен: ', BoolToStr(MyPair.Info, 'Да', 'Нет')); // Да
end.
```

---

## 🧠 Примеры использования в коллекциях

Так как `TPair` — это просто запись, её можно использовать в массивах, списках, словарях и т.д.

### ✅ Использование в списке TList<TPair>

```pascal
program PairListExample;

{$mode objfpc}{$H+}

uses
  Classes, SysUtils, Generics.Collections;

type
  TUserInfo = TPair<String, Integer, Boolean>; // псевдоним для удобства
var
  UserList: specialize TList<TUserInfo>;
  UserInfo: TUserInfo;

begin
  UserList := specialize TList<TUserInfo>.Create;

  // Добавляем несколько пользователей
  UserList.Add(TUserInfo.Create('Анна', 25, False));
  UserList.Add(TUserInfo.Create('Борис', 35, True));
  UserList.Add(TUserInfo.Create('Света', 28, True));

  for UserInfo in UserList do
  begin
    WriteLn(Format('Имя: %s, Возраст: %d, Активен: %s',
      [UserInfo.Key, UserInfo.Value, BoolToStr(UserInfo.Info, 'Да', 'Нет')]));
  end;

  UserList.Free;
end.
```

---

## 🧪 Пример: Использование TPair<TKey, TValue, TInfo> вместо класса

Вместо создания отдельного класса, можно использовать `TPair`:

```pascal
// Вместо:
type
  TUser = class
    Name: String;
    Age: Integer;
    IsActive: Boolean;
  end;

// Можно использовать:
type
  TUser = TPair<String, Integer, Boolean>;
```

Это легче, чем создавать класс, особенно если данные простые и не требуют методов.

---

## ⚠️ Ограничения

- `TPair` — это **запись**, а не класс, поэтому:
  - Нет наследования
  - Нет методов, кроме конструктора
  - Копируется по значению
- Не подходит для сложных объектов или когда нужна логика

---

## 🧩 Заключение

`TPair<TKey, TValue, TInfo>` — удобный способ группировать три значения разного типа, особенно при работе с коллекциями, где нужно хранить больше данных, чем просто пара `ключ-значение`.

Он помогает избежать создания отдельных классов/записей, если структура данных простая и не требует поведения.

---

Если тебе нужно больше примеров — например, как использовать `TPair` в `TDictionary` или `TObjectList`, — дай знать!