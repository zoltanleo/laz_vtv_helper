### USER

опиши подробно класс TList<T> из generics.Collections в fpc, покажи подробные примеры добавления и извлечения объектов с подробными пояснениями

### ASSISTANT

Класс `TList<T>` из модуля `Generics.Collections` в Free Pascal (FPC) — это **обобщённый (generic) динамический список**, который позволяет хранить элементы любого типа (`T`), будь то простые типы (например, `Integer`, `String`) или указатели на объекты. Он предоставляет удобный интерфейс для добавления, удаления, доступа к элементам и управления размером списка.

---

## 📦 Подключение

```pascal
uses
  Generics.Collections;
```

---

## 🧱 Основные свойства и методы `TList<T>`

| Свойство/метод        | Описание                                          |
| --------------------- | ------------------------------------------------- |
| `Add(Item)`           | Добавляет элемент в конец списка                  |
| `Insert(Index, Item)` | Вставляет элемент по указанному индексу           |
| `Delete(Index)`       | Удаляет элемент по индексу                        |
| `Remove(Item)`        | Удаляет первый найденный элемент                  |
| `Count`               | Количество элементов в списке                     |
| `Capacity`            | Текущая ёмкость списка                            |
| `Items[Index]`        | Доступ к элементу по индексу                      |
| `IndexOf(Item)`       | Возвращает индекс элемента или -1, если не найден |
| `Clear()`             | Очищает список                                    |
| `Contains(Item)`      | Проверяет, содержится ли элемент в списке         |

---

## ✅ Примеры работы с `TList<T>`

### 🔹 Пример 1: Работа с простыми типами (Integer)

```pascal
program ExampleIntegers;

{$mode objfpc}{$H+}

uses
  Classes, SysUtils, Generics.Collections;

var
  List: TList<Integer>;
  i: Integer;
begin
  // Создание списка
  List := TList<Integer>.Create;
  try
    // Добавление элементов
    List.Add(42);
    List.Add(100);
    List.Add(7);

    WriteLn('Список чисел:');
    for i := 0 to List.Count - 1 do
      WriteLn(List[i]);

    // Доступ к элементу по индексу
    WriteLn('Первый элемент: ', List[0]);
    WriteLn('Последний элемент: ', List[List.Count - 1]);

    // Поиск индекса
    WriteLn('Индекс числа 100: ', List.IndexOf(100));

    // Удаление элемента по значению
    List.Remove(100);

    WriteLn('После удаления 100:');
    for i := 0 to List.Count - 1 do
      WriteLn(List[i]);

  finally
    List.Free;
  end;
end.
```

#### 🔍 Объяснение:

- `List.Add(42)` — добавляет число в конец списка.
- `List[i]` — обращение к i-му элементу списка.
- `List.IndexOf(100)` — ищет элемент со значением 100 и возвращает его индекс.
- `List.Remove(100)` — удаляет первое вхождение значения 100.

---

### 🔹 Пример 2: Работа с объектами

```pascal
program ExampleObjects;

{$mode objfpc}{$H+}

uses
  Classes, SysUtils, Generics.Collections;

type
  TPerson = class
    Name: string;
    Age: Integer;
    constructor Create(AName: string; AAge: Integer);
  end;

constructor TPerson.Create(AName: string; AAge: Integer);
begin
  Name := AName;
  Age := AAge;
end;

var
  PersonList: TList<TPerson>;
  Person: TPerson;
begin
  PersonList := TList<TPerson>.Create;
  try
    // Создание и добавление объектов
    Person := TPerson.Create('Анна', 25);
    PersonList.Add(Person);

    Person := TPerson.Create('Борис', 30);
    PersonList.Add(Person);

    // Извлечение и вывод данных
    WriteLn('Список людей:');
    for Person in PersonList do
      WriteLn(Person.Name, ' - ', Person.Age);

    // Удаление объекта
    if PersonList.Count > 0 then
    begin
      PersonList[0].Free; // освобождаем память
      PersonList.Delete(0); // удаляем из списка
    end;

    WriteLn('После удаления первого элемента:');
    for Person in PersonList do
      WriteLn(Person.Name, ' - ', Person.Age);

  finally
    PersonList.Free;
  end;
end.
```

#### 🔍 Объяснение:

- `TPerson.Create(...)` — создаём объекты.
- `PersonList.Add(Person)` — добавляем ссылку на объект в список.
- `PersonList[0].Free` — **важно**: освобождаем память объекта **до** удаления из списка.
- `PersonList.Delete(0)` — удаляет элемент из списка по индексу.

---

## 🔄 Особенности при работе с объектами

- `TList<T>` **не освобождает** автоматически объекты при вызове `Clear()` или `Destroy()`.
- Вы должны **явно освобождать** каждый объект, например:

```pascal
procedure FreeList(AList: TList<TPerson>);
var
  Person: TPerson;
begin
  for Person in AList do
    Person.Free;
  AList.Clear;
end;
```

---

## 📌 Заключение

`TList<T>` — мощный и удобный инструмент для работы с коллекциями в FPC. Он обеспечивает:

- **Типобезопасность** благодаря обобщённым типам.
- **Динамическое изменение размера**.
- **Удобный интерфейс** для манипуляций с элементами.

Всегда следите за тем, чтобы **освобождать ресурсы**, особенно при работе с объектами, иначе возможны **утечки памяти**.

Если хочешь, могу показать примеры с `TObjectList<T>`, который автоматически управляет памятью объектов.